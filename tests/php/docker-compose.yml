version: "3"
services:
    run-tests:
        hostname: another-domain.intranet
        build: ./docker/
        volumes:
          - ./tests:/tests
          # For tests
          - ../data/acme.sh/mail.williamdes.eu.org/mail.williamdes.eu.org.cer:/etc/ssl/mail.williamdes.eu.org.cer:ro
        working_dir: /tests
        command: --no-configuration /tests

networks:
    default:
        external: true
        name: infrastructure_mail_infra_network
